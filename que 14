#include <stdio.h>
int main() {
    int num, sum = 0;
    float mean;
    for(int i = 1; i <= 10; i=i+1) {
        printf("Enter number %d:", i);
        scanf("%d", &num);
        sum += num;}
    mean = sum / 10.0;
    printf("Sum = %d\n", sum);
    printf("Mean = %.2f\n", mean);
    return 0;
}
